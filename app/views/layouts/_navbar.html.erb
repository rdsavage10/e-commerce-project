<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <%= link_to "Home", root_path, class: "navbar-brand" %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item dropdown  active">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Products
        </a>
        <div class="dropdown-menu active" aria-labelledby="navbarDropdown">
          <ul>
            <li><%= link_to 'All Products', root_path %></li>

            <div class="dropdown-divider"></div>

            <% @categories.each do |category| %>
              <li><%= link_to category.name, categorical_path(category_id: category.id) %></li>
            <% end %>
          </ul>
        </li>

        <li class="nav-item dropdown  active">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            Brand
          </a>
          <div class="dropdown-menu active wide-dropdown" aria-labelledby="navbarDropdown">

            <ul>
              <li><%= link_to 'All Brands', root_path %></li>
              <div class="dropdown-divider"></div>

              <% @brands.each do |brand| %>
                <li><%= link_to brand, branding_path(brand: brand) %></li>
              <% end %>

            </ul>
          </li>

        </ul>
        <form class="form-inline my-2 my-lg-0">
          <ul class="navbar-nav mx-auto">
            <li class="nav-item m-2">
              <i class="fa fa-shopping-cart"></i>
              <%= link_to "View Cart(#{@cart_count ? @cart_count : 0})", view_order_path %></li>
            <% if user_signed_in? %>

              <li class="nav-item m-2">
                Welcome!
                <%= current_user.email %></li>
              <li class="nav-item m-1"><%= link_to "Edit Profile", edit_user_registration_path, class: "btn btn-sm btn-warning" %></li>
              <li class="nav-item mx-2"><%= link_to("Sign Out", destroy_user_session_path, method: :delete, class: "btn btn-danger") %></li>

            <% else%>
              <%= link_to 'Sign In', new_user_session_path , class: "btn btn-primary" %>
            <% end %>
          </ul>
        </form>
      </div>
    </nav>
